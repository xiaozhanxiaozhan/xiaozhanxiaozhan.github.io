# AI 项目主页

一个使用 Vue 3、Vite 和 Vue Router 构建的明亮风格个人项目展示网站。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

生成的静态文件位于 `dist/`，可直接部署到 GitHub Pages。项目详情页使用哈希路由，兼容 GitHub Pages 的静态托管方式。
